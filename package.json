{
  "name": "claimbridge-cv",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web-portal"
  ],
  "scripts": {
    "dev": "npm --workspace apps/web-portal run dev",
    "build": "npm --workspace apps/web-portal run build",
    "lint": "npm --workspace apps/web-portal run lint"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
